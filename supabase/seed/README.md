# Seed data

## Copenhagen (development)

Run the SQL in `001_copenhagen.sql` in your Supabase project:

- Supabase Dashboard → **SQL Editor** → New query → paste file contents → Run

The script is idempotent for the city row and will replace the seeded venues by name.

